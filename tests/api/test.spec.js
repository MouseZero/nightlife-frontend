// const easyRequest = require('../../src/api/easy-requests')
import { expect } from 'chai'

describe('something', () => {
  it('test that chai works', () => {
    expect(true).to.equal(true)
  })
})
